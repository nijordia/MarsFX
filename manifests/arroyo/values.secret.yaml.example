# Arroyo secret values - copy to values.secret.yaml and fill in credentials
# Contains database credentials for external PostgreSQL

postgresql:
  externalDatabase:
    password: YOUR_POSTGRES_PASSWORD
